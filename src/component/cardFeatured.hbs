{{#with data}}

<div class="featured_card">
         <div class="featured_image "  style="background-image: url({{photo}}) ;"></div>
        
        <h2 class="featured_title">{{heading}}</h2>
        <p class="featured_text">{{info}}</p>
        <a href="{{href}}" class=" featured_button">{{linkText}}</a>
 </div>
  
    {{/with}}